apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-solr
  namespace: dotstat
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: dotstat-hostpath
  resources:
    requests:
      storage: 5Gi
  volumeName: pv-solr
